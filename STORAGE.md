# MINYAWE-LINK — التخزين (V5)

## الإعداد الحالي: سحابة + خزنة — بصمة ELMINYAWE بس اللي ظاهرة
1. **السحابة (أساسي)**: رفع مباشر لحد 200MB من غير حساب ومن غير فيزا — ملفات سنتين خمول (أي فتحة بتصفّر العداد).
2. **الخزنة (احتياطي)**: ديسك Railway `/data` — شغال دايما لو السحابة وقعت.
3. كل تخزين بياخد **محاولتين** قبل الانتقال للي بعده — الرفع ميفشلش من هزة عابرة.
4. اللي ظاهر للمستخدم لينكات موقعك بس: `/v` عرض + `/e` تشغيل + `/d` تحميل.

## التشغيل
في Railway → Variables:
```
ADMIN_TOKEN=باسورد_من_عندك
MAX_FILE_SIZE_MB=200
BASE_URL=https://<your-domain>.up.railway.app
DATA_DIR=/data
```
- `STORAGE_ORDER` (اختياري): مثلا `local,catbox` عشان تجرب الخزنة الأول.
- `STORAGE_DRIVER` (اختياري): `catbox` أو `local` لفرض تخزين واحد.

## ملاحظات
- الممنوع رفعه: exe/scr/bat/ps1/msi/dll/jar/com/vbs.
- أقصى مدة احتفاظ للينكات: شهر (من السيرفر). ملفات السحابة الخام عايشة حسب سياستها (سنتين خمول).
- النوع بيتخمن من الامتداد لو المتصفح بعت نوع عام — عشان الأغاني تشتغل مش تتحمل.
